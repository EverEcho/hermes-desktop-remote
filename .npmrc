# Upstream currently declares a React-streamdown peer range that is newer than
# its pinned Assistant UI package. Keep npm behavior aligned with the upstream
# lockfile until that dependency mismatch is resolved upstream.
legacy-peer-deps=true
