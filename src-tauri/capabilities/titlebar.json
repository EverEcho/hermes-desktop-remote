{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "titlebar",
  "description": "Allows the bundled main window to use the custom drag region.",
  "windows": ["main"],
  "permissions": ["core:window:allow-start-dragging"]
}
